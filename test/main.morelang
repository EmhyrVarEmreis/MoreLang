import util;

module main {

    public int c;

    public int fun(int a, int b) {
        int sum;
        sum = a + b;
        return sum*c;
    }

    public int fune(int a) {
        return a + 1;
    }

    public void main(int argc, string[] argv) {
        int y = fun(1,2);
        y = fune(y);
        print(int2string(y));
        return;
    }

}
